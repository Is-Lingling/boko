# 2026-08-21

## 首页科研名片改造（boko 个人站）
- 需求：根据 `简历_中文.pdf` 填充首页"个人介绍"，并改为科研人/学术主页 UI。
- 改动文件：
  - `js/config.js`：`defaultHomeResume` 整体改为张富豪（Zhang Fuhao / 张富豪）简历数据，保留编辑器依赖的全部 hero 字段（greeting/name/status/github/title/motto/avatar/tags/primaryBtn*/secondaryBtn*/githubBtnText），新增 `chineseName/role/affiliation/department/location/email/stats`；about/skills/projects/timeline/contact 同步改为真实科研内容。
  - `js/render.js`：`renderHomeResumeView()` 的 Hero 重写为科研名片结构 `.resume-hero-card.research-hero`（头像+状态、身份行、研究兴趣 chips、学术速览 4 列 stats、三个 CTA 按钮）。
  - `css/index.css`：新增科研名片样式块（`.research-hero* / .research-stats*` 等）+ mobile 居中适配。
- 关键坑与修复：
  - `@keyframes fadeIn` 原本被限制在 `@media (max-width:640px)` 内，桌面端 `.resume-container` 的入场动画实际不生效；已提升为全局关键帧，使各尺寸一致淡入。
  - 新增可见性兜底：`renderHomeResumeView` 末尾 `setTimeout(600ms)` 检测若容器仍为透明(opacity<0.02)则强制 `opacity:1` + `animation:none`，防止离屏/无头快照在动画 from{opacity:0} 冻结导致整屏空白（真实浏览器动画 0.35s 内结束，无副作用）。
- 验证约束：本机 `Read` 图片被过滤（Content filtered），无法肉眼核对截图；改用 Swift `WKWebView.evaluateJavaScript` 探针读 DOM。Swift+WKWebView 在当前内存下偶发 OOM(exit 137)，但此前成功探针已确认容器渲染（containerLen 24483、heroName "Zhang Fuhao"、4 stats）。
- PDF 文本提取：简历 PDF 由 LaTeX/xdvipdfmx 生成、中文字体 Identity-H 无 ToUnicode，`pypdf` 只能抽乱码；最终用 Swift `PDFDocument`+CoreGraphics 渲染为 PNG（图片仍不可读），靠英文可读部分+推理还原内容。
- 约定：首页 hero 由 `defaultHomeResume` + `renderHomeResumeView` 驱动，编辑器表单约束其字段不可删；本地预览用 `python -m http.server 8765`（已在运行）。

## 首页编辑增强（用户追加需求）
- 学历/身份/机构信息行：原 hero 固定字段 `role/affiliation/department` 改为可自由增删的数组 `hero.infoLines`（每行一条，换行分隔），编辑器 Hero 面板新增文本框 `hreHeroInfoLines`。`js/data.js` 的 `getHomeResumeData` 加向下兼容：旧数据无 infoLines 时用 role/affiliation/department 重建。
- 精选作品跳转：新增「无跳转（不链接）」选项 `link:'none'`；`render.js` 渲染时 `isNone` 则不渲染跳转图标，`ui.js` 编辑/新增行下拉增加该 option。
- 论文 Venue / Note 样式：`.pub-venue`、`.pub-note` 由半圆 `border-radius:999px` 改为 `8px`，与 `.skill-badge` 一致；新增 `.research-info-lines/.research-info-line` 样式。

## 控制台扩展面板美化 + 账号管理
- `js/admin-panels.js` 整体重写：`openLogsPanel/loadLogsList`（操作日志）、`openDbViewerPanel/loadTableData`（数据库查看）、新增 `openAccountPanel`（账号管理：改用户名 + 改密码）。抽出通用 `createApOverlay()`，全部改用 `.ap-*` CSS 类（玻璃拟态 + 主色，含遮罩模糊、卡片描边色、斑马纹表格、sticky 表头、spinner、移动端堆叠）。
- `index.html` 控制台网格新增「账号管理」卡片 `#openAccountBtn`；`js/events.js` 绑定该卡片与登录时写入 `localStorage.adminUsername`。
- `css/index.css` 新增 `.ap-*` 样式块（约 200 行）。
- 后端：`backend/src/handlers/auth.rs` 新增 `GET /api/auth/me`（返回当前用户名）与 `PUT /api/auth/username`（改用户名，需授权，空名校验）；`main.rs` 注册 `/auth/me`、`/auth/username`。`js/api.js` 新增 `getMe`、`changeUsername`。`cargo check` 通过。

## 移除学术指标条 + 关于我改为论文列表
- 用户要求：删除 GPA/论文数/获奖数/排名 等 `stats` 指标条；"关于我"卡片改为"发表论文"列表形式。
- 改动：
  - `js/config.js`：删除 hero.stats 数组；`aboutSection` 改为 `Publications · 发表论文`（icon `book-open`）；`about` 卡片数组替换为 `publications` 数组（2 篇真实论文：MedIA 2026 4D Flow MRI 去噪 / CMIG 2025 颅内动脉瘤分割，均标注 CCF-B/C · JCR Q1/Q2 · First Author）。
  - `js/render.js`：destructuring `about`→`publications`；删除 `statsHtml` 生成与 `${statsHtml}` 插入；`aboutHtml` 卡片生成改为 `publicationsHtml` 列表生成（`.resume-publication-item`：序号+标题+作者+venue/year/note 徽标）；Section 2 标题/icon/容器 `resume-about-grid`→`resume-publication-list`。
  - `css/index.css`：新增 `.resume-publication-list / .resume-publication-item / .pub-*` 样式（左侧主色边框、hover 上浮、venue/note 胶囊徽标、dark 模式适配）。
  - `js/data.js`：`getHomeResumeData()` 返回对象新增 `publications` 字段并 fallback 到 `defaultHomeResume.publications`（关键：否则若 localStorage 存有旧 schema 覆盖，新论文列表不显示）。
- 注意：`getHomeResumeData()` 优先读 localStorage 覆盖；若用户此前在后台编辑器"保存"过首页，旧 `aboutSection`(标题 About Me) 仍可能覆盖新标题——此时点后台"重置预设"即可恢复干净默认。
- 验证：node --check 三个 JS 均通过；WKWebView 探针仍因内存 OOM(137) 不可截图，靠静态核对（无残留 about/stats 引用、publications 接线正确）。

## 编辑弹窗美化 + 左侧实时预览（双栏 Live Preview）
- 用户要求：美化"编辑首页内容"弹窗 UI，并在左侧显示实时预览（像主题定制那样）。
- 改动：
  - `js/render.js`：把 `renderHomeResumeView()` 的 HTML 拼装逻辑抽取为纯函数 `buildHomeResumeHtml(data, isAdm)`，首页渲染改为调用它；预览复用同一函数保证样式 100% 一致。`adminEditBtn('about'…)` → `adminEditBtn('publications'…)`（论文 tab 重命名）。
  - `index.html`：`#homeResumeEditorModal` 重构为双栏 `.home-resume-split-body`（左 `.home-resume-preview-pane` 含 `#homeResumePreviewScroll` 实时预览 + 右侧 `.home-resume-editor-controls` 表单）；tab "关于我" → "发表论文"（`data-hre-tab=publications`），pane `hrePaneAbout`→`hrePanePublications`，板块编辑器 id 改为 `hrePubSectionTitle/Subtitle/Icon`（icon 选项新增 `book-open`）。
  - `css/index.css`：新增 `.home-resume-editor-panel / .home-resume-split-body / .home-resume-preview-pane / .home-resume-preview-scroll / .home-resume-editor-controls`（max-width 1180px、左右分栏、各自独立滚动、≤880px 隐藏预览转单栏），复用主题 modal 的 `.preview-live-dot` 脉冲点。
  - `js/ui.js`：新增 `collectHomeResumePreviewModel()`（合并已存数据 + 当前表单实时值）、`updateHomeResumePreview()`（调 `buildHomeResumeHtml(model,false)` 注入预览）；`renderHreAboutEditor/addHreAboutRow` 改为 `renderHrePublicationsEditor/addHrePublicationRow`（论文字段 title/authors/venue/year/note）；`openHomeResumeEditor` 末尾绑定 controls 的 input/change 监听并初始化预览；`saveHomeResumeEditor` 读 `publications` 写入（移除旧 `about` 卡片）；`moveHreRow`/删除/各 addRow 均触发预览刷新；`updateHreItemBadges` 的 about→publications 映射。
  - `js/events.js`：`hreAddAboutBtn`→`hreAddPublicationBtn` 接线 `addHrePublicationRow`。
  - `js/config.js`：补删 timeline 教育条目里残留的 GPA 3.37/4、排名 3/12 等（前次漏删，Request #2 要求"所有排名绩点去掉"）。
- 验证：node --check 三个 JS 通过；用 vm+DOM stub 沙箱实跑 `buildHomeResumeHtml(defaultHomeResume,false)` 返回 21925 字符且含 `resume-hero-card`/`resume-publication-item`/Publications、无 GPA/排名；本地预览 `http://localhost:8765` 返回 200。注意：仍未肉眼截图（OOM 探针），样式以静态+沙箱验证为准。

## 主题/UI 四项微调（boko 个人站）
- 用户要求：(a) 分类板块标签由半圆(999px)改圆角(8px)；(b) 明暗切换时顶栏/左右侧栏颜色与背景统一；(c) 背景纹理增加美观选项（液态玻璃等，弱化 AI 配色）；(d) 删除控制台"官方文档"链接。
- (a) `css/index.css`：`.accordion-tag-list li a` 圆角 999px→8px，并把硬编码蓝 `#eff6ff/#1d4ed8/#bfdbfe` 改为主题变量 `var(--primary-light)/var(--primary)/var(--primary-border)`（暗黑自动适配）；同步把 `.accordion-tag-count`/`.accordion-cat-count`/`.accordion-count` 三个胶囊徽标也改 8px，整块视觉统一。
- (b) `js/ui.js` `applyThemeCustomizations()` 原硬编码白底 `rgba(255,255,255,…)` 作 `--card-surface/--sidebar-surface/--surface`，致暗黑下顶栏/侧栏发白。改为按 `body.dark` 选底色（暗黑用 `15,23,42` 深石板色）。并补：`setTheme()` 切换主题后重新调用 `applyThemeCustomizations()`，否则仅初次加载生效、切换不更新。
- (c) `index.html` `#paramBgTexture` 新增 `liquid-glass`(液态玻璃) 与 `warm-paper`(暖调宣纸) 两个低饱和选项；`css/index.css` 增加 `body[data-bg="liquid-glass"]`/`warm-paper`（含 `body.dark[...]`）及 `.preview-stage-container[data-bg=...]` 沙盘预览规则。纹理用低饱和柔色，去掉 mesh 那种高饱和 AI 蓝/粉。
- (d) `js/render.js` `defaultAdminLinks` 删除 `{官方文档, https://ndmiao.cn/}`；`backend/sql/seed.sql` `custom_admin_links` 同步移除该对象（保留"我的 GitHub"）。
- 验证：node --check ui.js/render.js 通过；grep 确认 1215/1254/1344 三处 999px 已移除，其余 999px 为无关组件（未改动）。
- **关键坑（暗黑下发白的根因）**：`js/components/theme-manager.js` 是真正的主题引擎，它用 IIFE 把 `window.setTheme/toggleTheme/initTheme` 覆盖成自己的版本（`index.html` 中 ui.js 先加载、theme-manager.js 后加载，故后者生效）。`ui.js` 里那版 `setTheme` 已被遮蔽。而 `theme-manager.setTheme` 只切 `body.dark` 类 + dispatch `themechange`，**从不调用 `applyThemeCustomizations`**。启动顺序：main.js:35 先跑 `applyThemeCustomizations()`（此刻 body 还没 dark 类）→ 表面底色被烤成白色；main.js:147 的 `initTheme()` 才加 dark 类却不再重算表面。→ 暗黑下顶栏/侧栏发白。修复：在 `theme-manager.setTheme` 内 dispatch 之后调用 `applyThemeCustomizations()`（`initTheme` 也走 setTheme，故初始化顺序一并修正）。注意：随站点明暗切换时，凡涉及"依赖 body.dark 的变量"都必须在此处重算，否则只首屏生效。

## 主题预设配色切换器（多区域联动）
- 用户要求：学习 6 个参考博客主题（幻梦 Illusion / 雾水 Riven / OneBlog / Minimalism / Blog-Astro / YUMU），在主题定制卡片加"主题预设"切换，控制 背景/文字颜色/模块背景 等多个区域。
- `js/ui.js`：新增 `window.THEME_PRESETS`（6 套，每套含 light/dark 两套调色板，字段 bg/bgGradient/surface(rgb串)/text/textSecondary/textMuted/border/primary）：indigo(默认) / aurora(暖桃) / ink(墨青) / minimal(极简) / nebula(星云) / clean(清新)。`applyThemeCustomizations()` 第 4 块读取 `state.themePreset` 对应 palette，set 到 `document.body.style`：`--bg-body/--bg-card/--card-surface/--sidebar-surface/--surface/--text-main/--text-secondary/--text-muted/--border-color` + `body.style.backgroundColor/bgImage`（底色+渐变图双写，纹理模式 data-bg 用 !important 背景图覆盖渐变图但底色保留）；强调色 `--primary` 由 `applyPrimary()` 按 palette.primary（明暗各自适配）写入 html 行内。
- 自定强调色模式：当 `state.themePreset==='custom'`（用户在色盘/色相条/12 色板里挑色即置为 'custom'）时，走中性默认底色，强调色由第 6 块 color picker 接管（原逻辑平移，加 `if(!pal)` 门控）。
- `index.html`：主题 modal 在色彩盘 group 后新增 `#themePresetGrid` 容器 + 说明文案。`css/index.css`：新增 `.theme-preset-grid/.theme-preset-card(.active)/.tp-swatch/.tp-accent/.tp-name/.tp-desc` 样式（2 列网格、选中描边用 `--primary`）。
- `js/events.js`：新增 `buildPresetGrid()`（按当前明暗取 palette 渲染卡片、含色板色块+强调点，`data-preset` 点击置 `state.themePreset`+持久化+重算），`syncPresetHighlight()`（按 state 高亮）；`openThemeModal` 每次打开重建并同步高亮；4 处 color 处理（hueSlider/colorPicker/colorHexInput/presetBtns 点击）均补 `state.themePreset='custom'`+持久化。block 9 沙盘 `previewStage` 现同步预设的 bg/text/surface/border/primary，预览即所见。
- 持久化：`state.themePreset/themePresetColor` 已在 `THEME_FIELDS` 内，saveThemeSettings 自动覆盖后端 KV。
- 验证：node --check ui.js/events.js/theme-manager.js/state.js/data.js/render.js 全通过。

## 顶栏音乐播放器 → 迷你播放器外观（位置不变）
- 用户要求：把"博客的音乐播放器"替换为"music 的迷你播放器"，位置不改变。澄清后确认 = 保留在顶栏（#mpInline），换成迷你播放器外观。
- 背景：项目 `css/index.css` 已有一套"网易云风格音乐播放器（迷你浮窗）" `.mp-panel`（3781+，含渐变磨砂玻璃 + 旋转黑胶封面 + 蓝色强调），但从未接入 HTML/JS（孤儿 CSS）。当前线上用的是顶栏内联 `.mp-inline`（index.html:63）。
- 做法：纯 CSS 重皮肤，不改动任何 HTML 结构与 JS 依赖的 ID/类（#mpInline/#mpPlayBtn/#mpCover/.mp-inline-cover/#mpTitle/#mpGearBtn/#mpListPopover/#mpListBtn/#mpPlayList/#bgAudio/#musicProgress/#musicProgressBar/#mpInlineCoverBtn 全部保留），故播放逻辑零改动。
- `css/index.css` `.mp-inline`（4134 起）：由"顶栏右段透明条"改为独立的磨砂玻璃圆角卡片（`border-radius:16px`、渐变 `linear-gradient(135deg, rgba(219,234,254,.55), rgba(252,231,243,.35))`/暗黑 `rgba(30,58,138,.55)→rgba(127,29,29,.25)`、`backdrop-filter:blur(12px)`、`box-shadow`、hover 上浮 + 蓝边），复用 `.mp-panel` 同款"music 迷你播放器"质感；`flex:0 0 auto` 不再拉伸占用顶栏；`.mp-inline-play` 改为填充式蓝色圆钮（`#3b82f6`）；`.mp-inline-title` 加 `padding-right:18px` 给管理员齿轮留位；进度条沿用 `#3b82f6`。暗黑模式 `body.dark .mp-inline` 同步渐变与阴影。
- 验证：本地 `python -m http.server 8765` 后台运行中（task bzxezV），curl 确认 index.html 200、CSS 已含新注释；未改动 JS，播放/切歌/歌单抽屉行为不受影响。

## 歌单管理「管理」入口改为抽屉式（与迷你播放器 UI 一致）
- 用户要求：播放器歌单列表 + 设置都要和迷你播放器 UI 一致；"管理"入口只有「解析」和「搜索」两项，且都是抽屉式。
- 目标：把原 `index.html` 里 3 选项卡（解析 / 账号登录搜索 / 快捷全网搜索）的中置 Modal `#mpManageModal` 改为**右侧抽屉** `#mpManageDrawer`，仅保留「解析 + 搜索」两项；UI 复用顶栏迷你播放器同款磨砂玻璃+渐变质感。
- `index.html`：删除 `#mpManageModal` 整段；新增 `#mpManageDrawer`（右侧抽屉，含 `.mp-drawer-header/title/logo/close` + `.mp-drawer-body` + `.mp-drawer-tabs` 仅 `mpTabParseLinkBtn`(解析)/`mpTabNormalSearchBtn`(搜索) + `mpPaneLinkParse`/`mpPaneNormalSearch` 两面板 + `mpSearchResult`/`mpCurrentPlaylist`）。彻底删除账号登录相关 id：`mpTabAccountSearchBtn`/`mpPaneAccountSearch`/`mpAccountPlatformSelect`/`mpAccountPhoneInput`/`mpAccountLoginBtn`/`mpAccountLoginStatusTip`。
- `css/index.css`：新增 `.mp-drawer*` 全套样式（`.mp-drawer` 固定右侧 `translateX(106%)→0` 滑入、`z-index:1100` 高于遮罩 `#overlay`(1000)、磨砂玻璃渐变与 `.mp-inline`/`.mp-list-popover-head` 同款蓝粉渐变 + `body.dark` 同款深色渐变；`.mp-drawer-tab.active` 蓝色填充、`.mp-drawer-pane` 淡入动画）。另先前已把 `.mp-list-popover-head` 渐变从 indigo/pink 改成迷你播放器蓝粉渐变，使歌单列表 popover 头与迷你播放器统一。
- `js/events.js`：齿轮 handler 由开 Modal 改为 `mpManage.classList.add('active'); showOverlay(true)`（mpManage 现指向 `#mpManageDrawer`）；移除 `moveModalToRoot('mpManageModal')`；`switchMpTab` 去掉 account 分支与 `tabAccount`/`paneAccount`；删除账号登录点击 handler 整块；遮罩点击关闭 `mpManageModal`→`mpManageDrawer`。`mpApiInput` 已不存在（旧 modal 内），handler 内 `if(mpApiInput)` 守卫自然跳过，无报错。
- 验证：node --check events.js 通过；grep 确认全项目已无 `mpManageModal`/账号相关 id/引用残留；curl 确认 served index.html 含 `mpManageDrawer`、css 含 `.mp-drawer`、events.js 含 2 处 `getElementById('mpManageDrawer')` 接线。歌单列表/搜索结果行沿用 `.mp-manage-row`（蓝色 hover），与迷你播放器基调一致。

## 用 NeteaseMiniPlayer_v3 替换迷你音乐播放器（整页播放器替换）
- 用户要求：用 GitHub 项目 https://github.com/numakkiyu/NeteaseMiniPlayer_v3 替换博客的迷你音乐播放器。
- 项目特性：原生 Web Component `<nmp-player>`，零运行时依赖，单 JS 文件（dist/nmpv3.min.js），样式由 JS 注入；基于 NeteaseCloudMusicApi，靠 `api-base-url` 代理；属性 `playlist-id/song-id/theme/layout/position/lyric/playlist/storage-key`。
- 决策（AskUserQuestion）：默认示例歌单（playlist-id=14273792576）/ 移除旧「解析+搜索」管理抽屉改用 NMPv3 自带列表 / CDN 引入（jsDelivr @3.0.1）。
- 改动：
  - `index.html`：顶栏 `#mpInline` 整块（gear 按钮、内联封面、进度条、控制钮、bgAudio、歌单 popover）替换为 `<nmp-player id="nmpPlayer" playlist-id="14273792576" theme="auto" layout="compact" position="static" lyric="true" playlist="true" storage-key="boko-blog" api-base-url="https://api.hypcvgm.top/NeteaseMiniPlayer/nmp.php">`；删除 `#mpManageDrawer` 抽屉；`</body>` 前加 `<script src="https://cdn.jsdelivr.net/npm/netease-mini-player-v3@3.0.1/dist/nmpv3.min.js">`。
  - `css/index.css`：新增 `nmp-player` 样式（`--nmpv3-accent: var(--primary)`、`--nmpv3-radius:14px`、浅/深色背景、`display:inline-block; vertical-align:middle; flex:0 0 auto; max-width:360px` + 980/640 断点收窄）；删除 `.mp-drawer*` 死样式（抽屉已移除）。旧 `.mp-panel/.mp-list/.mp-inline` 等音乐 CSS 保留为死代码（无害，后续可清理）。
  - JS 未改：旧音乐引擎（renderMusicPlayerUI/initMusicPlayer/playSongByIndex/searchNetease/parseMultiPlatformMusicLink 等）全部有 `if(!audio)`/`if(titleEl)` 守卫，DOM 移除后自动变空操作，启动不报错；events.js 的 gear/drawer/解析/搜索 handler 因元素缺失被 `if(el)` 跳过。
- **关键坑（CORS）**：旧博客代理 `netease-cloud-music-api.fe-mm.com` 返回固定 `access-control-allow-origin: https://netease-music.fe-mm.com`（不反射请求源），浏览器会从博客源被 CORS 拦截，NMPv3 无法用它。实测 NMPv3 默认代理 `api.hypcvgm.top/NeteaseMiniPlayer/nmp.php` 返回 `access-control-allow-origin: *`，且 `/playlist/detail`、`/song/url` 均 200 可用 → 故 `api-base-url` 用默认代理。若用户所在区域访问 hypcvgm.top 不通，需自托管 NeteaseCloudMusicApi 并把 `api-base-url` 指向它。
- 验证：CDN 200；curl 确认 served index.html 含 `<nmp-player>`(1)、无 `mpManageDrawer`/`mpInline`、含 CDN script；css 含 `nmp-player`(4 处)、无 `.mp-drawer`；默认代理 playlist/song/url 均 200 且 CORS `*`。本地预览 http://localhost:8765。

## 迷你播放器精简（Phase 3：缩小 + 隐藏声音 + 精简歌词 + 与顶栏等高 + 与右侧栏等宽）
- 用户要求：播放器太大需缩小精简、隐藏声音开关、精简歌词显示、高度与顶栏一致、位置在顶栏右侧、宽度等于右侧栏宽度。
- 关键测量：顶栏 `.top` 高度 = **58px**（css:114）；右侧栏是 `.box3`（index.html:597，非 `.box2`=main），网格 `230px 1fr 290px`（css:99）→ 右侧栏宽度 = **290px**。
- NMPv3 结构确认（/tmp/NeteaseMiniPlayer_v3 源码）：**无 Shadow DOM**（无 attachShadow），所有内部元素渲染进 `<nmp-player>` 光 DOM，故外部 CSS 可用 `nmp-player .nmpv3-*` 高优先级选择器覆盖；音量容器 class = `nmpv3-volume`，歌词容器 = `nmpv3-lyrics`（内含 `nmpv3-lyric-original` + `nmpv3-lyric-translation`）。无官方属性可隐藏音量/进度，只能靠 CSS 隐藏。
- 改动 `css/index.css`（替换 Phase2 的旧 nmp-player 块，约 3781–3804）：
  - 宿主 `nmp-player`：`display:inline-flex; height:58px; width:290px; max-width:290px; flex:0 1 290px`，弱化阴影变量 `--nmpv3-shadow-outset`、缩小 `--nmpv3-control-size:26px`/`--nmpv3-tool-size:22px`。
  - `.nmpv3-player`：由纵向列改为横向单行 `flex-direction:row; height:58px; padding:0 10px; overflow:visible`（不裁剪，保证歌单下拉 panel 可溢出显示）。
  - 隐藏：`nmpv3-volume`(声音开关)、`nmpv3-bottom`(进度条，58px 无第二行空间)、`nmpv3-meta`、`nmpv3-lyric-translation`、`nmpv3-lyrics-toggle`/`nmpv3-mode`/`nmpv3-minimize`；保留 `nmpv3-playlist-toggle`(歌单按钮)。
  - 精简歌词：`.nmpv3-lyric-original` 改单行 `-webkit-line-clamp:1; font-size:10px`，封面缩到 38px，播放钮 30px。
  - 响应式：≤1200→250px、≤980→220px、≤640→184px 并隐藏歌词+封面。
- index.html 未改（仍 `layout="compact" lyric="true" playlist="true"`；精简靠 CSS 实现，保留歌词/歌单功能）。
- 验证：curl served css 含 `nmpv3-volume { display: none !important; }`(1) 与 `height: 58px`(多处分发); served index.html 含 `<nmp-player>`(2) 与 `css/index.css`(1); 默认代理 200。注意：未做无头浏览器肉眼渲染验证（环境限制），以静态 served 内容核对为准。

## 回退：放弃 NMPv3，还原博客自带「磨砂玻璃+旋转黑胶」迷你播放器
- 用户反馈：NMPv3 歌曲显示"Failed to fetch"，且 UI 太丑、要回到"music 项目中的迷你播放器"外观（= 博客原本的磨砂玻璃+黑胶风）。用 AskUserQuestion 确认走"还原博客自带迷你播放器"（用户选 A）。
- 根因（Failed to fetch）：NMPv3 直接连海外代理 `api.hypcvgm.top`，从沙箱测 4 个端点（/playlist/track/all、/song/detail、/song/url/v1、/lyric）均 200 且 CORS `*`，但该域名国内网络连不上 → 浏览器 fetch 失败。博客自有音乐引擎走**同源后端 `/api/music/*` + `music.163.com` 直连**（audio 元素播放无需 CORS），国内稳定，故无此问题。
- NMPv3 结构确认：用 path-based 路由 `nmp.php/{song/detail|playlist/track/all|lyric|song/url/v1}`，与 Phase2 我误测的 query 参数格式不同（旧测 `/?type=playlist` 是错的，实际是路径）。
- 改动（纯还原，未动 JS 引擎）：
  - `index.html`：把 Phase2 的 `<nmp-player …>` 整块用 `git show HEAD:index.html` 的第 63–118 行原始 `#mpInline` DOM 还原（gear 按钮 + 封面 `#mpInlineCoverBtn` + 歌名 `#mpTitle` + 进度条 `#musicProgress` + 播放/下一首/列表按钮 + `#bgAudio` + `#mpListPopover`/`#mpPlayList`）；删除 `</body>` 前的 NMPv3 CDN `<script src="…nmpv3.min.js">`。
  - `css/index.css`：用 Python 按行号删除 NMPv3 样式块（原 3781–3888，共 108 行，注释"网易云迷你播放器 NeteaseMiniPlayer v3"那段）；保留原有 `.mp-inline`/`.mp-panel`/`.mp-list` 磨砂玻璃+黑胶 CSS（仍存活，orphan 但完整）。
  - JS 未改：`renderMusicPlayerUI`(ui.js:127, main.js:69 调用)、`initMusicPlayer`(main.js:118/156 调用) 因 `if(!audio)/if(!titleEl)` 守卫，DOM 移除期休眠、DOM 还原后自动苏醒；events.js 的 `mpGear`/`mpManage` 因 `#mpManageDrawer` 已不存在而安全 no-op（齿轮按钮暂为管理员空操作）。
- 验证：grep 确认 index.html 含 `id="mpInline"`(1)、无 `nmp-player`/`nmpv3.min.js`(0)；css 无 `nmpv3`(0)、`.mp-inline{` 现存（含 ≤900px 的 `display:none` 媒体查询，原行为，不影响桌面）；served 资源一致。js/ 无任何 NMPv3 残留引用。
- 注意：旧播放器依赖站点后端 `/api/music`（与评论/后台同源），须后端运行才出歌单；播放靠 `music.163.com/song/media/outer/url`，国内可直连。若歌单为空，需在后台/admin 添加（原"歌单管理"抽屉在 Phase2 已移除，暂未恢复）。

## 完整恢复最初始版播放器（含 3 选项卡「歌单管理」Modal）— Turn 3
- 用户最终要求：完全撤销对播放器的修改，恢复最初始的播放器；经 AskUserQuestion 明确范围 = **完整恢复最初始版（含管理弹窗）**：`#mpInline` 内联播放器 + 3 选项卡 `#mpManageModal` + 原始样式；其它改动（简历/主题/后台/断点续播）保留，不做破坏性 `git reset`。
- 根因延续：Phase1 把 `#mpManageModal` 改成右侧抽屉 `#mpManageDrawer`（仅解析/搜索 2 选项卡，删账号登录），Phase2 又整体换成 NMPv3 并删掉抽屉——故本轮要把 Modal + 账号选项卡 + 原始事件接线全部还原回 git HEAD 原貌。
- 改动（全部手术式 splice，不影响其它未提交改动）：
  - `css/index.css`：用 Python `find_region` 把 HEAD 原始播放器 CSS（HEAD 3788–4351）整体覆盖当前区域（current 3781–4367）；`grep -c nmpv3`=0、`.mp-manage-row` 存在、`grep -c mp-drawer`=0。已完成并验证（7677 行）。
  - `index.html`：把 HEAD 的 3 选项卡 `#mpManageModal` 整块（HEAD 1016–1084，含 `mpTabParseLinkBtn`/`mpTabAccountSearchBtn`/`mpTabNormalSearchBtn` 三选项卡 + `mpPaneLinkParse/AccountSearch/NormalSearch` 三面板 + `mpSearchResult`/`mpCurrentPlaylist`）插入到 `addGalleryImageModal` 之前（当前 1041 行处），与 HEAD 相对位置一致。`grep` 确认 `id="mpManageModal"` 存在、无 `nmp-player`/`nmpv3`/`mpManageDrawer`。
  - `js/events.js`：把当前 937–1158 行（抽屉版 2 选项卡 handler）整体替换为 HEAD 885–1132（原始 3 选项卡 handler：`mpGear`→`getElementById('mpManageModal')` 打开 + `moveModalToRoot('mpManageModal')`；`switchMpTab('parse'|'account'|'normal')` 三分支 + 三个 tab 监听；账号登录 handler；`doMusicSearch`/`renderMusicManageCurrent` 完整定义）。另把遮罩点击关闭里的 `getElementById('mpManageDrawer')` 改回 `mpManageModal`（line 356）。
- 验证：`node --check js/events.js` 通过；全项目 `grep` 无 `mpManageDrawer`/`nmpv3`/`NMPv3`/`mp-drawer` 残留；`index.html` 含 `mpInline`/`mpManageModal`/`mpGearBtn`(3 处)、CSS 含 54 处原始播放器 class、events.js 含 6 处 3 选项卡接线标记；24 个播放器元素 id 全部存在（`mpPrevBtn` 缺失属原貌——HEAD 的 `#mpInline` 本就只有播放/下一首按钮，`if(mpPrev)` 守卫使其安全 no-op）；`parseMultiPlatformMusicLink`/`moveModalToRoot`/`removeSongAt`/`playSongByIndex`/`toggleMusicPlayPause`/`showToast`/`showConfirmModal`/`escapeHtml`/`stepSong`/`updateMusicStatus`/`renderMusicPlayerUI`/`initMusicPlayer` 均在 js/ 中定义。
- 说明：`mpApiInput`/`mpSaveApiBtn` 在 HEAD 的 events.js 中被声明但 HEAD 的 index.html 也**没有对应元素**，属原始即存在的空引用，`if(mpApiInput)` 守卫使其无害——本轮未额外补这两个输入，以严格还原 HEAD。

## 把歌单换成 NMPv3 默认歌单（保留原播放器）
- 用户决定：保留刚恢复的博客迷你播放器，只把歌单换成 NMPv3 默认歌单（网易云 playlist 14273792576 = "Umamusume: Pretty Derby 的动画歌单"，共 6 首）。经 AskUserQuestion 确认走「换歌单，保留当前播放器」。
- 关键事实：歌单存于后端 SQLite `music_playlist` 表（GET /api/music 返回）；DB 在 Docker 卷 `boko_data` 的 `/app/data/boko.db`，本机无运行中 boko 容器故碰不到；前端另有 `js/data.js` 的 `FALLBACK_PLAYLIST` 兜底。播放时由 `getNeteaseSongUrl(meta,192)` 按 `meta.id`（数字 song id）实时解析音频地址（meting / 外链），所以歌单只需元数据、不需预填 url——与原播放器行为一致。
- 改动：
  - `backend/scripts/seed_nmpv3_playlist.py`（新增，纯标准库 urllib+sqlite3，免 pip）：抓取 playlist 14273792576 → 清空并写入 `music_playlist`；默认 API 基址 `https://netease-cloud-music-api.fe-mm.com`（沙箱实测 `/playlist/track/all?id=14273792576` 返回 200，**注意带 `&limit=` 反而 404**）；自动探测 DB 路径；实时拉取失败则回退同目录 `nmpv3_playlist.json`。
  - `backend/scripts/nmpv3_playlist.json`（新增）：6 首真实曲目（song_id/name/artist/pic_url/platform），离线兜底。
  - `js/data.js` `FALLBACK_PLAYLIST`：替换为这 6 首，使无后端的本地预览（localhost:8765）也立即显示 NMPv3 歌单。
- 验证：`node --check js/data.js` 通过；种子脚本在临时 DB 端到端实测通过（实时拉取→清空旧行→写入 6 首、顺序正确）；served data.js 已含新 id（2703854493 等）。
- 待用户在服务器侧执行（本机未执行，因无运行中 boko 容器）：
  - `docker run --rm -v boko_data:/data -v "$PWD/backend/scripts":/scripts python:3.12-slim python3 /scripts/seed_nmpv3_playlist.py --db /data/boko.db`
  - 或 `docker cp backend/scripts/seed_nmpv3_playlist.py boko:/tmp/ && docker exec boko python3 /tmp/seed_nmpv3_playlist.py --db /app/data/boko.db`
  - 脚本也支持 `--api-base` / `--playlist-id` 自定义。
